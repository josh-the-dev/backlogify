{
  "name": "backlogify",
  "private": true,
  "packageManager": "npm@10.9.4",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "biome check .",
    "format": "biome check --write .",
    "test": "turbo run test"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.0",
    "turbo": "^2.0.0"
  },
  "overrides": {
    "srvx": "0.8.15"
  },
  "dependencies": {
    "srvx": "0.8.15"
  }
}
